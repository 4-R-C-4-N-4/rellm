"""Concept-direction homology module (docs/homology/proposal.md).

Derived model, not a trained one: extracted concept directions per
(concept, tradition) cell in a base model's residual stream, plus the
geometry over them. Direction sets are keyed by (model id, extraction date)
— never compare directions across different extractions.
"""
